package com.example.pedrobraga.bancofinanca;

import android.arch.lifecycle.LiveData;
import android.arch.lifecycle.MutableLiveData;
import android.content.Context;

import com.example.pedrobraga.bancofinanca.Entity.Local;
import com.example.pedrobraga.bancofinanca.Repository.LocalRepository;

import org.junit.Test;
import org.junit.runner.RunWith;

import java.util.ArrayList;
import java.util.List;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */


public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() throws Exception {


     /*   Local local = new Local();

        local.setDesclocal("Mercado Boni");


        LocalRepository repo = new LocalRepository();
        repo.insert(local);

        LiveData<List<Local>> livelocal = new MutableLiveData<List<Local>>();

        List<Local> locais = new ArrayList<Local>();
        locais.addAll(livelocal.getValue());

        assert(!locais.isEmpty());*/
    }
}